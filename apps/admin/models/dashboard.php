<?php // Site admin dashboard model

ipsCore::requires_model( 'model' );

class admin_dashboard_model extends admin_model {

	// Construct
	public function __construct( $model ) {
		parent::__construct( $model );

	}

}
